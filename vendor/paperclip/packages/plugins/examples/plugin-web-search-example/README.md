# Web Search Plugin

First-party Paperclip plugin that exposes a lightweight `web-search` tool for agents.

The worker performs a public web search against DuckDuckGo's HTML endpoint and
returns a compact set of ranked results with titles, URLs, and snippets.
